# 阶段 4：扩展 UI 与配置管理验收报告

验收时间：2026-08-02（Asia/Shanghai）

## 结论

阶段 4 已通过。程序现已具备延迟加载的轻量设置页、深浅主题、编辑字体与字号配置、可关闭的轻量过渡动画，以及集中式 INI 配置管理。阶段 3 和阶段 2 完整功能回归、阶段 1 全量性能回归、独立部署验证均通过。按更新后的迁移文档，本阶段未实现历史草稿、标签页、固定草稿、多光标、插件或 LSP，也未恢复已由用户明确排除的 Markdown 预览。

## 实现范围

- 新增集中式 `AppSettings` 配置层，统一管理窗口几何、快捷键、主题、编辑字体、字号和动画开关。
- 正式配置使用 `QStandardPaths::AppConfigLocation/settings.ini`；测试支持独立 `SCRATCHEDITOR_SETTINGS_FILE`，不会污染用户配置。
- 配置文件包含 schema 版本；首次使用新的正式配置文件时，会从旧的 Windows Native Settings 迁移窗口几何和快捷键，旧数据仍保留。
- 设置页通过 QML `Loader` 首次调用时创建，关闭后释放；默认快捷键为 `Ctrl+,`，也可从命令面板打开。
- 支持 `dark` / `light` 两套完整界面配色；Markdown 高亮格式与 Win32 DWM 暗色属性同步切换。
- 编辑字体必须是系统已安装字体；字号限制为 9–24。默认仍为 `Microsoft YaHei UI`、13 pt。
- 主题、字体、字号、动画、窗口几何和自定义快捷键均跨进程持久化，并提供恢复默认外观。
- 使用 `Behavior`、`ColorAnimation` 和 `NumberAnimation` 实现 120 ms 入场与颜色过渡；关闭动画后时长为 0。未加入循环动画或空闲轮询。

## 功能与回归验收

最终结果文件：`artifacts/baselines/stage4-results-20260802-130258.json`

| 检查项 | 结果 |
|---|---:|
| 集中配置文件初始化与 schema | 通过 |
| 默认主题、字体、字号和动画 | 通过 |
| 设置页按需加载与关闭释放 | 通过 |
| 浅色主题、字体、字号和动画开关应用 | 通过 |
| 非法主题、缺失字体和越界字号拒绝 | 通过 |
| 外观、快捷键和窗口几何跨进程恢复 | 通过 |
| 所有配置组集中在同一 INI 文件 | 通过 |
| 阶段 3 完整回归 | 通过 |
| 阶段 2 完整回归 | 通过 |
| Markdown 预览及文档明确暂缓功能 | 均未注册、显式拒绝 |
| Qt WebEngine / WebView / Chromium 引用 | 无 |
| 原始 AHK 文件与仓库状态 | 均未变化 |
| 已运行用户实例保护 | 通过，PID `25844`、`39288` 全程保留 |

原始 `KeysRedirect.ahk` 验收前后 SHA-256 均为：

`8BB8FFEFEBD9A6C90C102F66583D517C6C5CF83D36200A3D4E77D413C77B41C9`

## 阶段 1 性能回归

最终回归结果：`artifacts/baselines/stage4-performance-20260802-130336.json`

| 指标 | 最终实测 | 门槛 | 结果 |
|---|---:|---:|---:|
| 新进程可用最大值 | 82.84 ms | ≤ 300 ms | 通过 |
| 热唤醒 P95 | 25.96 ms | ≤ 50 ms | 通过 |
| 10 万字输入到帧 P95 | 16.15 ms | ≤ 16.667 ms | 通过 |
| 空闲 CPU | 0% | ≤ 0.5% | 通过 |
| 隐藏工作集 | 39.50 MB | ≤ 80 MB | 通过 |
| 动画 | 60.16 FPS / P95 16.85 ms | ≥ 55 FPS / ≤ 20 ms | 通过 |
| 10 万字加载 | 34.91 ms | 无明显阻塞 | 通过 |
| 微软拼音真实候选提交 | `你好` | 精确中文提交 | 通过 |

性能运行时确认设置页保持未加载，新增配置和 UI 代码没有进入热输入路径。11 个命令、原生 Markdown 高亮、暗色首帧、CJK 字形和高 DPI 合约均同时通过。

## 构建与交付

- 阶段 4 Release 产物位于 `build/stage4/ScratchEditor.exe`，SHA-256 为 `8C577DF076151B7E93E98FF0974DC890D3475CC3FF6E568848B953FF33E3BDA9`。
- `build/stage4` 已通过 `windeployqt` 部署，共 160 个文件、约 53.50 MB。
- 在移除 Qt/MinGW 工具链 PATH 后，独立启动、集中配置初始化、设置页延迟加载、IPC 和正常退出均通过。
- 构建预设：`cmake --build --preset stage4`；功能验收入口：`scripts/run-stage4-tests.ps1`；性能回归沿用 `scripts/run-stage1-tests.ps1` 的隔离参数。
- Qt 部署资源仍输出已知的 `libpng iCCP` 警告，不影响运行或验收结果。
- 当前正在运行的阶段 3 与阶段 2 用户实例未覆盖；阶段 4 产物已在独立目录就绪。

## 下一阶段

更新后的阶段 5 是项目收尾：清理目录、集中测试与工作文档、更新项目文档、初始化 Git 仓库并提交。本报告完成后暂停，不提前执行阶段 5。阶段 6 涉及移除原 AHK 编辑器模块，必须等待用户明确批准。
